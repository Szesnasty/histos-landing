/**
 * The open/closed line.
 *
 * The open column is deliberately specific and the commercial column is
 * deliberately broad. That asymmetry is the point: the open source has to look
 * checkable, and the commercial layer has to read as a real plan without being a
 * published roadmap.
 */
export const OPEN_SOURCE_CAPABILITIES: string[] = [
  'Histos Python runtime',
  'The policy format',
  'Deterministic policy evaluation',
  'Argument and resource checks',
  'Output controls and redaction',
  'Local audit trail',
  'CLI and developer workflow',
]

export const COMMERCIAL_CAPABILITIES: string[] = [
  'Centralized operations',
  'Fleet-wide visibility',
  'Enterprise workflows',
  'Long-term audit and governance',
]
