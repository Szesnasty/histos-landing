{
  "name": "histos-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "npm run sync:spec && nuxt generate",
    "preview": "nuxt preview",
    "sync:spec": "node scripts/sync-spec.mjs",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^4.0.0",
    "sass-embedded": "^1.100.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  }
}
